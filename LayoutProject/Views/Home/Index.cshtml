@{
    ViewBag.Title = "Index";
}
<app-root (onMenuClicked)="onMenuClicked($event)" style="min-height: 300px">
    <div class="alert alert-info" role="alert">
        Loading… <span class="glyphicon glyphicon-refresh" aria-hidden="true"></span>
    </div>
</app-root> 